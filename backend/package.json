{
  "name": "backend",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch todos.json --no-cors true --port 9000"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
